#!/usr/bin/env bash
# SUPERSEDED by scripts/round5_weekend.sh (doctor | preflight | launch).
# Kept as a stub so nothing half-working stays runnable.
echo "weekend_run.sh is superseded -- use: bash scripts/round5_weekend.sh doctor" >&2
exit 2
